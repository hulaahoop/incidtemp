class MyClass {
  set(k, one) {
    // Method implementation
    // Typically, you would do something with parameters k and one here
  }
}

// Usage:
const myObject = new MyClass();
myObject.set("key", 1);
